# CI / Production env example for `webapp-admin`
VITE_API_BASE=${{ secrets.VITE_API_BASE }}
VITE_SENTRY_DSN=${{ secrets.VITE_SENTRY_DSN }}

# Note: Admin UIs should require auth; keep server-side secrets out of frontend envs.
