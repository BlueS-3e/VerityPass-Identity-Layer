Cleanup performed — archived duplicates and pointers

Summary

- Removed/archived duplicate Solidity sources to avoid ambiguity and reduce accidental compilation of outdated copies.
  - Archived copies moved to: archive/duplicates/
    - webapp_RealMintLaunchpad.sol (from webapp/contracts/RealMintLaunchpad.sol)
    - contracts_root_MockV3Aggregator.sol (from contracts/MockV3Aggregator.sol)
    - mocks_MockV3Aggregator.sol (from contracts/mocks/MockV3Aggregator.sol)
    - contracts_root_MockERC20.sol (from contracts/MockERC20.sol)
    - mocks_MockERC20.sol (from contracts/mocks/MockERC20.sol)
    - webapp_RealMintLaunchpad.json (artifact from webapp/build/contracts/RealMintLaunchpad.json)

- Replaced the original duplicate files with small pointer stubs (archival markers) so builds use the canonical sources under `contracts/contracts/`.

Why

- Multiple copies of the same contract existed in different locations (webapp and root). This caused ambiguity and risk where Hardhat could compile an outdated copy by mistake.
- Archiving preserves history while avoiding accidental use of stale code.

How to re-generate artifacts after cleanup

- Contracts (Hardhat)
  - cd contracts
  - npm install
  - npx hardhat compile --force
  - npx hardhat test

- Webapp (if you need the webapp artifact)
  - cd webapp
  - npm install
  - npm run build (or the project-specific build command)

Notes and recommendations

- I left canonical contract sources at `contracts/contracts/` (these are the active sources for compilation and tests).
- If you'd like, I can also archive or remove `contracts/artifacts/` and `contracts/cache/` to reduce repo size. These are regenerated by Hardhat and typically excluded from source control.
- Consider adding an explicit CONTRIBUTING.md or developer README that documents where the canonical sources live and the commands to regenerate artifacts.

Files changed/created by cleanup

- archive/duplicates/* (new)
- webapp/contracts/RealMintLaunchpad.sol (replaced with pointer)
- contracts/MockV3Aggregator.sol (replaced with pointer)
- contracts/MockERC20.sol (replaced with pointer)
- contracts/mocks/MockV3Aggregator.sol (replaced with pointer)
- contracts/mocks/MockERC20.sol (replaced with pointer)
- webapp/build/contracts/RealMintLaunchpad.json (shrunk to a pointer)
- CLEANUP.md (new)

If you want, I can now:
- Remove `contracts/artifacts/` and `contracts/cache/` (archive or delete).
- Remove `webapp/build/` and `frontend/build/` or move them to archive.
- Add a `.gitignore` update to exclude build artifacts and caches.
- Run backend pytest and build the frontend to confirm everything still works.

Tell me which of those (if any) you'd like me to do next and I'll proceed.
